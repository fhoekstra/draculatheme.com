import "./index.scss";

import Particles from "../particles";

const Hero = () => {
  return (
    <section className="hero">
      <Particles className="particles" />
    </section>
  );
};

export default Hero;
